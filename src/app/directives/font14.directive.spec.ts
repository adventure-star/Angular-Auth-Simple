import { Font14Directive } from './font14.directive';

describe('Font14Directive', () => {
  it('should create an instance', () => {
    const directive = new Font14Directive();
    expect(directive).toBeTruthy();
  });
});
